# k_means_clustering
K means clustering in an unsupervised learning algorithm and the data points are divided into k clusters and each cluster has a centroid and the data points lie near the centroid 
wcss should be lower so that it ensures high efficiency of the algorithm
